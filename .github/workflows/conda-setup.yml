name: Conda setup

on: [push]

jobs:
  test_conda:
    name: Example (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.9", "3.11"]
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
      - name: Install Our own environment
        shell: bash -el {0}
        run: |
          conda env create --name anuga_env --file environments/environment_${{matrix.python-version}}.yml --prune
      - name: Install package
        shell: bash -el {0}
        run: |
          conda activate anuga_env
          conda info
          pwd
          conda list
          pip install .
      - name: Test package
        shell: bash -el {0}
        run: |
          conda list
          python --version
          pytest -q --pyargs anuga